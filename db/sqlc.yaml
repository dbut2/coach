version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        sql_package: "pgx/v5"
        out: "../go/database"
        emit_interface: true
        emit_json_tags: true
        overrides:
          - db_type: int
            go_type: int
